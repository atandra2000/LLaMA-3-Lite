"""In-tree ``shared_data`` package for LLaMA-3-Lite.

Import surface directly from ``shared_data.loader`` (see ``dataset.py``);
this marker keeps the vendored package importable via ``sys.path``.
"""
